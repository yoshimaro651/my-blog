---
import { getCollection } from 'astro:content';
import FormattedDate from '../../components/FormattedDate.astro';

export async function getStaticPaths() {
  const posts = await getCollection('blog');
  return posts.map((post) => ({
    params: { slug: post.id.replace('.md', '') },
    props: { entry: post },
  }));
}

const { entry } = Astro.props;
const { Content } = await entry.render();

const allPosts = await getCollection('blog');
const relatedPosts = allPosts
  .filter(p => p.id !== entry.id)
  .slice(0, 3);
---

<!doctype html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <title>{entry.data.title} | IT活用のススメ</title>
    <meta name="description" content={entry.data.description} />
    
    <!-- SEO強化 -->
    <meta property="og:title" content={entry.data.title} />
    <meta property="og:description" content={entry.data.description} />
    <meta property="og:type" content="article" />
    <meta property="og:url" content={`https://itkatsyo.com/blog/${entry.id.replace('.md', '')}/`} />
    <meta name="twitter:card" content="summary_large_image" />
    
    <link rel="canonical" href={`https://itkatsyo.com/blog/${entry.id.replace('.md', '')}/`} />
    
    <style>
      /* 省略（前回と同じCSS変数・ヘッダー・ナビ） */
      :root {
        --bg-primary: #f8fafc;
        --bg-surface: #ffffff;
        --text-primary: #0f172a;
        --text-secondary: #64748b;
        --text-muted: #94a3b8;
        --border: #e2e8f0;
        --primary: #1e40af;
        --primary-light: #3b82f6;
        --primary-hover: #1d4ed8;
        --success: #059669;
        --shadow-md: 0 4px 6px -1px rgb(0 0 0 / 0.1), 0 2px 4px -2px rgb(0 0 0 / 0.1);
        --shadow-lg: 0 20px 25px -5px rgb(0 0 0 / 0.1);
        --shadow-xl: 0 35px 60px -12px rgb(0 0 0 / 0.15);
      }

      * { margin: 0; padding: 0; box-sizing: border-box; }
      
      body {
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Noto Sans JP', 'Hiragino Kaku Gothic ProN', Meiryo, sans-serif;
        line-height: 1.75;
        color: var(--text-primary);
        background: var(--bg-primary);
        font-weight: 400;
      }

      /* ヘッダー（下余白追加） */
      header {
        background: linear-gradient(135deg, var(--primary) 0%, var(--primary-light) 100%);
        color: white;
        padding: 5rem 2rem 2rem; /* ↓ 下余白を減らしてナビとの間隔調整 */
        position: relative;
        overflow: hidden;
        margin-bottom: 2rem; /* ↓ ヘッダー下に余白追加 */
      }
      
      header::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: url('data:image/svg+xml,<svg width="60" height="60" xmlns="http://www.w3.org/2000/svg"><rect width="1" height="1" fill="white" opacity="0.08"/></svg>');
        opacity: 0.3;
      }
      
      .header-content {
        max-width: 1200px;
        margin: 0 auto;
        position: relative;
        z-index: 1;
        text-align: center;
      }

      /* ナビゲーション */
      nav {
        background: var(--bg-surface);
        box-shadow: var(--shadow-md);
        position: sticky;
        top: 0;
        z-index: 100;
        backdrop-filter: blur(10px);
        margin-bottom: 3rem; /* ↓ ナビ下に余白追加 */
      }
      
      .nav-content {
        display: flex;
        justify-content: center;
        gap: 1.5rem;
        max-width: 1200px;
        margin: 0 auto;
        padding: 1rem 2rem;
      }
      
      .nav-link {
        color: var(--text-primary);
        text-decoration: none;
        font-weight: 500;
        padding: 0.75rem 1.5rem;
        border-radius: 12px;
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      }
      
      .nav-link:hover {
        background: linear-gradient(135deg, var(--primary-light), var(--primary-hover));
        color: white;
        transform: translateY(-2px);
        box-shadow: var(--shadow-lg);
      }

      /* 記事コンテナ */
      .article-container {
        max-width: 900px;
        margin: 0 auto;
        padding: 0 2rem 4rem;
      }

      /* 記事ヘッダー（上余白大幅追加） */
      .article-header {
        text-align: center;
        margin-bottom: 4rem;
        padding: 4rem 0 3rem; /* ↑ 上余白を4remに増加 */
        border-bottom: 3px solid var(--border);
        background: linear-gradient(180deg, transparent 0%, rgba(248,250,252,0.8) 100%);
        border-radius: 24px 24px 0 0;
        margin-top: 2rem; /* ↑ さらに上余白追加 */
      }
      
      .article-title {
        font-size: clamp(2.25rem, 5vw, 3.75rem);
        font-weight: 800;
        line-height: 1.2;
        margin-bottom: 2rem; /* ↓ タイトル下余白増加 */
        background: linear-gradient(135deg, var(--primary), var(--primary-light));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
      }
      
      .article-meta {
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 2.5rem; /* ↓ アイコン間隔拡大 */
        flex-wrap: wrap;
        color: var(--text-secondary);
        font-size: 1.05rem;
        margin-bottom: 1rem;
      }
      
      .meta-item {
        display: flex;
        align-items: center;
        gap: 0.75rem;
        padding: 0.75rem 1.25rem;
        background: rgba(255,255,255,0.6);
        border-radius: 12px;
        backdrop-filter: blur(10px);
      }
      
      .meta-icon {
        width: 1.375rem;
        height: 1.375rem;
        opacity: 0.8;
      }

      /* 本文エリア */
      .article-content {
        background: var(--bg-surface);
        border-radius: 0 0 24px 24px; /* ↑ ヘッダーと一体化 */
        padding: 4rem;
        box-shadow: var(--shadow-xl);
        margin-bottom: 5rem; /* ↓ 下余白増加 */
        border: 1px solid var(--border);
        border-top: none;
      }
      
      /* 以下、既存スタイル（見出し・本文など）は省略 */
      .article-content :global(h2) {
        font-size: 2rem;
        margin: 4rem 0 2rem;
        font-weight: 700;
        color: var(--text-primary);
        padding-left: 1.5rem;
        border-left: 5px solid var(--primary-light);
      }
      
      /* アクション */
      .article-actions {
        display: flex;
        gap: 1.5rem;
        justify-content: center;
        flex-wrap: wrap;
        margin: 5rem 0 3rem;
      }
      
      .action-button {
        padding: 1.125rem 2.25rem;
        border-radius: 16px;
        font-weight: 600;
        text-decoration: none;
        transition: all 0.3s ease;
        display: inline-flex;
        align-items: center;
        gap: 0.75rem;
        font-size: 1.05rem;
        box-shadow: var(--shadow-lg);
      }
      
      .back-button {
        background: var(--bg-surface);
        color: var(--primary-light);
        border: 2px solid var(--primary-light);
      }
      
      .back-button:hover {
        background: var(--primary-light);
        color: white;
        transform: translateY(-4px);
        box-shadow: var(--shadow-xl);
      }

      /* レスポンシブ */
      @media (max-width: 768px) {
        .article-header {
          padding: 3rem 1rem 2.5rem;
          margin-top: 1.5rem;
        }
        
        .article-meta {
          gap: 1.5rem;
          flex-direction: column;
        }
        
        .article-content {
          padding: 2.5rem 1.75rem;
          margin-bottom: 3rem;
        }
      }
    </style>
  </head>
  <body>
    <!-- ヘッダー・ナビ（前回と同じ） -->
    <header>
      <div class="header-content">
        <h1 class="site-title">IT活用のススメ</h1>
        <p class="site-description">思考と仕事をITで時短していく</p>
      </div>
    </header>

    <nav>
      <div class="nav-content">
        <a href="/" class="nav-link">🏠 ホーム</a>
        <a href="/category/tech" class="nav-link">💻 Tech</a>
        <a href="/category/web" class="nav-link">🌐 Web</a>
        <a href="/sitemap" class="nav-link">🗺️ サイトマップ</a>
      </div>
    </nav>

    <div class="article-container">
      <!-- 記事ヘッダー（上余白大幅強化） -->
      <div class="article-header">
        <h1 class="article-title">{entry.data.title}</h1>
        <div class="article-meta">
          <div class="meta-item">
            <svg class="meta-icon" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm1-12a1 1 0 10-2
