---
import { getCollection } from 'astro:content';
import FormattedDate from '../../components/FormattedDate.astro';

export async function getStaticPaths() {
  const posts = await getCollection('blog');
  return posts.map((post) => ({
    params: { slug: post.id.replace('.md', '') },
    props: { entry: post },
  }));
}

const { entry } = Astro.props;
const { Content } = await entry.render();

const allPosts = await getCollection('blog');
const relatedPosts = allPosts
  .filter(p => p.id !== entry.id)
  .slice(0, 3);
---

<!doctype html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{entry.data.title} | IT活用のススメ</title>
    <meta name="description" content={entry.data.description} />
    
    <!-- SEO強化 -->
    <meta property="og:title" content={entry.data.title} />
    <meta property="og:description" content={entry.data.description} />
    <meta property="og:type" content="article" />
    <meta property="og:url" content={`https://itkatsyo.com/blog/${entry.id.replace('.md', '')}/`} />
    <meta name="twitter:card" content="summary_large_image" />
    
    <link rel="canonical" href={`https://itkatsyo.com/blog/${entry.id.replace('.md', '')}/`} />
    
    <style>
      :root {
        --bg-primary: #f8fafc;
        --bg-surface: #ffffff;
        --text-primary: #0f172a;
        --text-secondary: #64748b;
        --text-muted: #94a3b8;
        --border: #e2e8f0;
        --primary: #1e40af;
        --primary-light: #3b82f6;
        --primary-hover: #1d4ed8;
        --success: #059669;
        --shadow-md: 0 4px 6px -1px rgb(0 0 0 / 0.1), 0 2px 4px -2px rgb(0 0 0 / 0.1);
        --shadow-lg: 0 20px 25px -5px rgb(0 0 0 / 0.1);
        --shadow-xl: 0 35px 60px -12px rgb(0 0 0 / 0.15);
      }

      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }
      
      body {
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Noto Sans JP', 'Hiragino Kaku Gothic ProN', Meiryo, sans-serif;
        line-height: 1.75;
        color: var(--text-primary);
        background: var(--bg-primary);
        font-weight: 400;
        overflow-x: hidden;
      }

      /* ヘッダー（トップページと統一・SVG修正） */
      header {
        background: linear-gradient(135deg, var(--primary) 0%, var(--primary-light) 100%);
        color: white;
        padding: 3.5rem 1.5rem 2.5rem;
        position: relative;
        overflow: hidden;
      }
      
      header::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: url("image/svg+xml,%3Csvg width='60' height='60' xmlns='http://www.w3.org/2000/svg'%3E%3Crect width='1' height='1' fill='white' opacity='0.08'/%3E%3C/svg%3E");
        opacity: 0.3;
        pointer-events: none;
      }
      
      .header-content {
        max-width: 1200px;
        margin: 0 auto;
        position: relative;
        z-index: 1;
        text-align: center;
      }

      .site-title {
        font-size: clamp(1.75rem, 8vw, 3rem);
        font-weight: 800;
        margin-bottom: 0.5rem;
        letter-spacing: -0.025em;
        background: linear-gradient(135deg, white 0%, rgba(255,255,255,0.9) 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
      }

      .site-description {
        font-size: clamp(1rem, 4vw, 1.2rem);
        opacity: 0.95;
        font-weight: 300;
      }

      /* ナビゲーション（スマホハンバーガー対応） */
      nav {
        background: var(--bg-surface);
        box-shadow: var(--shadow-md);
        position: sticky;
        top: 0;
        z-index: 100;
        backdrop-filter: blur(10px);
      }
      
      .nav-content {
        max-width: 1200px;
        margin: 0 auto;
        padding: 0.75rem 1.5rem;
      }
      
      /* デスクトップナビ */
      .desktop-nav {
        display: flex;
        justify-content: center;
        gap: 1rem;
      }
      
      .nav-link {
        color: var(--text-primary);
        text-decoration: none;
        font-weight: 500;
        padding: 0.5rem 1rem;
        border-radius: 12px;
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        position: relative;
        white-space: nowrap;
        font-size: 0.95rem;
      }
      
      .nav-link:hover,
      .nav-link:active {
        background: linear-gradient(135deg, var(--primary-light), var(--primary-hover));
        color: white;
        transform: translateY(-2px);
        box-shadow: var(--shadow-lg);
      }
      
      /* ハンバーガーメニューボタン */
      .mobile-menu-btn {
        display: none;
        background: none;
        border: none;
        font-size: 1.5rem;
        cursor: pointer;
        padding: 0.5rem;
        color: var(--text-primary);
      }

      /* 記事コンテナ */
      .article-container {
        max-width: 900px;
        margin: 0 auto;
        padding: 0 1.5rem 4rem;
      }

      /* 記事ヘッダー */
      .article-header {
        text-align: center;
        margin-bottom: 3rem;
        padding: 3rem 0 2rem;
        border-bottom: 3px solid var(--border);
        background: linear-gradient(180deg, transparent 0%, rgba(248,250,252,0.8) 100%);
        border-radius: 24px 24px 0 0;
        margin-top: 1.5rem;
      }
      
      .article-title {
        font-size: clamp(1.875rem, 6vw, 3rem);
        font-weight: 800;
        line-height: 1.2;
        margin-bottom: 1.25rem;
        background: linear-gradient(135deg, var(--primary), var(--primary-light));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
      }
      
      .article-meta {
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 1.5rem;
        flex-wrap: wrap;
        color: var(--text-secondary);
        font-size: 0.95rem;
        margin-bottom: 1.5rem;
      }
      
      .meta-item {
        display: flex;
        align-items: center;
        gap: 0.5rem;
      }
      
      .meta-icon {
        width: 1.125rem;
        height: 1.125rem;
        opacity: 0.7;
      }

      /* 本文エリア */
      .article-content {
        background: var(--bg-surface);
        border-radius: 24px;
        padding: 3rem 2rem;
        box-shadow: var(--shadow-lg);
        margin-bottom: 3rem;
        border: 1px solid var(--border);
      }
      
      /* 見出し */
      .article-content :global(h2) {
        font-size: clamp(1.5rem, 4vw, 2rem);
        margin: 3rem 0 1.5rem;
        font-weight: 700;
        color: var(--text-primary);
        padding-left: 1rem;
        border-left: 4px solid var(--primary-light);
      }
      
      .article-content :global(h3) {
        font-size: clamp(1.25rem, 3.5vw, 1.5rem);
        margin: 2.25rem 0 1rem;
        font-weight: 600;
        color: var(--text-primary);
      }
      
      /* 段落 */
      .article-content :global(p) {
        margin-bottom: 1.5rem;
        font-size: 1.05rem;
        line-height: 1.8;
      }
      
      /* リスト */
      .article-content :global(ul),
      .article-content :global(ol) {
        margin: 1.25rem 0;
        padding-left: 1.75rem;
      }
      
      .article-content :global(li) {
        margin-bottom: 0.5rem;
        line-height: 1.7;
      }
      
      /* リンク */
      .article-content :global(a) {
        color: var(--primary-light);
        text-decoration: none;
        font-weight: 500;
        border-bottom: 2px solid transparent;
        transition: all 0.2s ease;
      }
      
      .article-content :global(a:hover),
      .article-content :global(a:active) {
        color: var(--primary-hover);
        border-bottom-color: var(--primary-light);
      }
      
      /* コード */
      .article-content :global(code) {
        background: #f1f5f9;
        padding: 0.2rem 0.5rem;
        border-radius: 6px;
        font-size: 0.9em;
        color: var(--primary);
        border: 1px solid var(--border);
        font-family: 'SF Mono', Monaco, 'Cascadia Code', monospace;
      }
      
      .article-content :global(pre) {
        background: linear-gradient(135deg, #1e293b 0%, #334155 100%);
        color: #e2e8f0;
        padding: 1.75rem;
        border-radius: 12px;
        overflow-x: auto;
        margin: 1.75rem 0;
        box-shadow: var(--shadow-xl);
        border: 1px solid #475569;
      }
      
      /* 画像 */
      .article-content :global(img) {
        max-width: 100%;
        height: auto;
        border-radius: 12px;
        margin: 2rem auto;
        display: block;
        box-shadow: var(--shadow-lg);
        border: 1px solid var(--border);
      }
      
      /* 引用 */
      .article-content :global(blockquote) {
        border-left: 5px solid var(--primary-light);
        padding: 1.5rem 1.75rem;
        margin: 2.5rem 0;
        background: linear-gradient(135deg, #eff6ff 0%, #dbeafe 100%);
        border-radius: 12px;
        font-style: italic;
        color: var(--text-primary);
        box-shadow: var(--shadow-md);
      }
      
      /* テーブル */
      .article-content :global(table) {
        width: 100%;
        border-collapse: collapse;
        margin: 2rem 0;
        border-radius: 12px;
        overflow: hidden;
        box-shadow: var(--shadow-md);
        background: var(--bg-surface);
      }
      
      .article-content :global(th),
      .article-content :global(td) {
        padding: 1rem;
        border: 1px solid var(--border);
        text-align: left;
      }
      
      .article-content :global(th) {
        background: linear-gradient(135deg, #f8fafc 0%, #f1f5f9 100%);
        font-weight: 600;
      }

      /* アクション・関連記事 */
      .article-actions {
        display: flex;
        gap: 1rem;
        justify-content: center;
        flex-wrap: wrap;
        margin: 3rem 0 2rem;
      }
      
      .action-button {
        padding: 0.875rem 1.75rem;
        border-radius: 12px;
        font-weight: 600;
        text-decoration: none;
        transition: all 0.3s ease;
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        font-size: 0.95rem;
      }
      
      .back-button {
        background: var(--bg-surface);
        color: var(--primary-light);
        border: 2px solid var(--primary-light);
        box-shadow: var(--shadow-md);
      }
      
      .back-button:hover,
      .back-button:active {
        background: var(--primary-light);
        color: white;
        transform: translateY(-2px);
        box-shadow: var(--shadow-xl);
      }
      
      /* 関連記事 */
      .related-section {
        background: var(--bg-surface);
        border-radius: 24px;
        padding: 2.5rem;
        margin: 3rem 0;
        box-shadow: var(--shadow-lg);
        border: 1px solid var(--border);
      }
      
      .related-title {
        font-size: clamp(1.375rem, 4vw, 1.75rem);
        font-weight: 700;
        margin-bottom: 1.75rem;
        text-align: center;
        color: var(--text-primary);
      }
      
      .related-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
        gap: 1.25rem;
      }
      
      .related-card {
        padding: 1.25rem;
        border-radius: 16px;
        background: #f8fafc;
        border: 1px solid var(--border);
        transition: all 0.3s ease;
      }
      
      .related-card:hover,
      .related-card:active {
        transform: translateY(-4px);
        box-shadow: var(--shadow-lg);
        background: var(--bg-surface);
      }
      
      .related-card h3 {
        font-size: 1.1rem;
        margin-bottom: 0.5rem;
        color: var(--text-primary);
        line-height: 1.35;
      }
      
      .related-meta {
        color: var(--text-muted);
        font-size: 0.85rem;
      }

      /* フッター（簡易版） */
      footer {
        background: linear-gradient(180deg, #111827 0%, #1f2937 100%);
        color: #9ca3af;
        padding: 3rem 1.5rem 2rem;
        text-align: center;
      }
      
      .footer-content p {
        font-size: 0.875rem;
        opacity: 0.8;
      }

      /* スマホ最適化 */
      @media (max-width: 768px) {
        header {
          padding: 2.5rem 1rem 2rem;
        }
        
        .nav-content {
          padding: 0.75rem 1rem;
        }
        
        /* ハンバーガーメニュー */
        .desktop-nav {
          display: none;
        }
        
        .mobile-menu-btn {
          display: block;
        }
        
        .article-container {
          padding: 0 1rem 3rem;
        }
        
        .article-header {
          padding: 2rem 0 1.5rem;
          margin-top: 1rem;
        }
        
        .article-content {
          padding: 2rem 1.25rem;
          margin-bottom: 2.5rem;
        }
        
        .article-actions {
          flex-direction: column;
          gap: 0.75rem;
        }
        
        .action-button {
          justify-content: center;
          width: 100%;
          max-width: 280px;
        }
        
        .related-grid {
          grid-template-columns: 1fr;
          gap: 1rem;
        }
        
        .related-section {
          padding: 2rem 1.5rem;
          margin: 2.5rem 0;
        }
      }

      /* 超小型スマホ */
      @media (max-width: 480px) {
        .article-container,
        header,
        .nav-content,
        footer {
          padding-left: 1rem;
          padding-right: 1rem;
        }
        
        .article-content {
          padding: 1.75rem 1rem;
        }
        
        .related-card {
          padding: 1rem;
        }
      }
    </style>
  </head>
  <body>
    <header>
      <div class="header-content">
        <h1 class="site-title">IT活用のススメ</h1>
        <p class="site-description">思考と仕事をITで時短していく</p>
      </div>
    </header>

    <nav>
      <div class="nav-content">
        <div class="desktop-nav">
          <a href="/" class="nav-link">🏠 ホーム</a>
          <a href="/category/tech" class="nav-link">💻 Tech</a>
          <a href="/category/web" class="nav-link">🌐 Web</a>
          <a href="/sitemap" class="nav-link">🗺️ サイトマップ</a>
        </div>
        <button class="mobile-menu-btn" aria-label="メニュー">☰</button>
      </div>
    </nav>

    <div class="article-container">
      <div class="article-header">
        <h1 class="article-title">{entry.data.title}</h1>
        <div class="article-meta">
          <div class="meta-item">
            <svg class="meta-icon" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm1-12a1 1 0 10-2 0v4a1 1 0 00.293.707l2.828 2.829a1 1 0 101.415-1.415L11 9.586V6z" clip-rule="evenodd"></path>
            </svg>
            <FormattedDate date={entry.data.pubDate} />
          </div>
          <div class="meta-item">
            <svg class="meta-icon" fill="currentColor" viewBox="0 0 20 20">
              <path d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
            {entry.data.category}
          </div>
          <div class="meta-item">
            <svg class="meta-icon" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-6.5-2.5a.75.75 0 011.5 0v3.5a.75.75 0 01-.468.69l2.12 1.23a.75.75 0 11-.894 1.2l-2.416-1.402A.75.75 0 017.5 12.75V9.5z" clip-rule="evenodd"></path>
            </svg>
            {Math.round(entry.data.description.length / 20)}分程度
          </div>
        </div>
      </div>

      <article class="article-content">
        <Content />
      </article>

      <div class="article-actions">
        <a href="/" class="action-button back-button">
          ← 記事一覧に戻る
        </a>
      </div>

      {relatedPosts.length > 0 && (
        <div class="related-section">
          <h2 class="related-title">関連記事</h2>
          <div class="related-grid">
            {relatedPosts.map((post) => (
              <a href={`/blog/${post.slug}/`} class="related-card">
                <h3>{post.data.title}</h3>
                <div class="related-meta">
                  <FormattedDate date={post.data.pubDate} /> • {post.data.category}
                </div>
              </a>
            ))}
          </div>
        </div>
      )}
    </div>

    <footer>
      <div class="footer-content">
        <p>&copy; 2026 IT活用のススメ. All rights reserved.</p>
      </div>
    </footer>

    <script>
      // スマホハンバーガーメニュー
      const mobileBtn = document.querySelector('.mobile-menu-btn');
      const desktopNav = document.querySelector('.desktop-nav');
      
      mobileBtn.addEventListener('click', () => {
        const isVisible = desktopNav.style.display === 'flex';
        desktopNav.style.display = isVisible ? 'none' : 'flex';
        if (!isVisible) {
          desktopNav.style.flexDirection = 'column';
          desktopNav.style.gap = '0.5rem';
          desktopNav.style.background = 'var(--bg-surface)';
          desktopNav.style.padding = '1rem';
          desktopNav.style.position = 'absolute';
          desktopNav.style.top = '100%';
          desktopNav.style.left = '0';
          desktopNav.style.right = '0';
          desktopNav.style.boxShadow = 'var(--shadow-md)';
          desktopNav.style.zIndex = '99';
          desktopNav.style.borderRadius = '0 0 12px 12px';
        }
      });

      // ナビ外クリックで閉じる
      document.addEventListener('click', (e) => {
        if (!nav.contains(e.target)) {
          desktopNav.style.display = 'none';
        }
      });
    </script>
  </body>
</html>
